trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: dotnet build ShiftEntity.VSIX.Template --configuration release --output $(Build.SourceDirectory)/bin/Release
  displayName: 'dotnet build '
  continueOnError: true

- task: PublishVisualStudioExtension@4
  displayName: "Publish Extension to Visual Studio Marketplace"
  inputs:
    connectedServiceName: 'marketplaceServiceConnection'
    vsixFile: '$(Build.SourceDirectory)/bin/Release/ShiftEntity.VSIX.Template.vsix'
    manifestFile: '$(Build.SourceDirectory)/bin/Release/publishManifest.json'
    publisherId: 'shiftsoftware'

